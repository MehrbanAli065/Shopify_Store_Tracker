# dependencies
node_modules/

# local database (PGlite file store — rebuilt with `npm run reset`)
tracker/data/

# secrets — never commit these
.env
.env.local
client_secrets.json
mycreds.txt
*.pem

# deployment
.vercel/

# scraped data — CSVs land here daily, they belong on Drive, not in git
*.csv

# editor / OS
.claude/settings.local.json
.DS_Store
Thumbs.db

.vercel
.env*
!.env.example
!tracker/.env.example

# local deploy output
deploy.log

# generated VM transfer archive
shopify-tracker-vm.zip

# handover notes — holds live credentials, hand it over directly
mehrban.txt
mehrban.html

# local copy of the database dump — 295 MB, and the server keeps its own nightly
_deploy/
